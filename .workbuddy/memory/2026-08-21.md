# 2026-08-21

## NexCard 项目评审与规划（未改代码）

- 通读了项目全部核心源码（widget/mysql/myserial/各业务页），输出方案文档 `docs/NexCard_优化与功能规划方案.md`。
- 关键结论：
  - P0：管理员密码明文、`IN (...)` SQL 拼接、数值字段 TEXT 存储、考勤状态中文硬编码。
  - P1：登录会话依赖全局 `MySerial::cardId`、串口类 include UI 头文件（分层倒置）、子窗口缺 WA_DeleteOnClose、无 schema 迁移机制。
  - P2：COM5 硬编码、空壳文件 refill/userpagewidget 未清理、表格全量加载。
- 人脸识别专项方案：推荐 OpenCV YuNet 检测 + ArcFace ONNX 特征 + ONNX Runtime，刷卡定位 + 人脸 1:1 双因子打卡；employee 表加 face_feature BLOB + face_registered；4~6 周分阶段实施路线已写入文档。
- 用户只要求方案，未修改任何代码。

## 项目代码备份

- 将整个项目（85 个文件，3.0MB）完整复制到同级备份目录 `E:\Qt_code\code\C++\Project\Smart_Card_Pass_backup_20260821_1641`，diff -r 校验与源目录完全一致。

## 优化第一批：管理员权限体系（已实现+编译验证通过）

1. **admin 表加 level 字段**（1=一级管理员，2=二级管理员）：
   - `mysql.cpp` creatMySql：建表语句加 `level INTEGER NOT NULL DEFAULT 2`；新增旧库迁移（PRAGMA table_info 检查→ALTER TABLE ADD COLUMN）；root 用 `INSERT OR IGNORE` + `UPDATE` 强制为一级管理员。
   - `insertAdminData(name, pwd, level=2)` 默认二级，注册流程（adminregisterwidget）无需改动。
2. **新增管理员列表页** `adminofadmin.{h,cpp,ui}`：表格显示 用户名+权限等级（不查 pwd 列），搜索按用户名模糊匹配，一级排前；已挂到 adminpagewidget 菜单"管理员信息管理→管理员列表"（懒加载+关闭清理均已接入），.pro 已登记。
3. **验证**：Qt 5.12 mingw shadow build 编译链接成功生成 NexCard.exe；Python sqlite3 模拟旧库迁移/新注册/列表查询全部通过。
- 环境：Qt 工具链在 `E:/Qt/Qt5.12.0`（qmake + mingw730_64）。

## 优化第二批：三级管理员权限体系（已实现+编译验证通过）

**权限规则**：1=一级（root 唯一）2=二级 3=三级。
- 一级（root）：注册管理员 + 修改其他管理员权限为二级/三级
- 二级：仅注册管理员
- 三级：仅查看管理员列表

**改动**：
- `mysql.h/cpp`：insertAdminData 默认 level=3；新增 `findAdminLevel(name)`、`updateAdminLevel(name,newLevel)`（root 与非法等级拒绝，只允许 2/3）；建表/迁移 DEFAULT 改 3。
- `loginadminwidget`：loginOK 信号带参 (name, level)；登录成功查 level 并发出。
- `widget.cpp`：lambda 接收 name/level → `aw->setLoginAdmin(name, level)`。
- `adminpagewidget`：新增 setLoginAdmin（三级隐藏"管理员注册"菜单）；"管理员注册"点击加三级拦截；创建 AdminOfAdmin 时传登录级别。
- `adminofadmin`：UI 新增"修改权限"按钮（仅一级可见）；点击后单选一行 → root 禁止修改 → QInputDialog 选二级/三级 → updateAdminLevel → 刷新。

**验证**：shadow build 编译链接成功；Python 验证 root 不可改/非法等级拒绝/一级唯一/权限矩阵全部通过。

## 优化第三批：管理员列表复选框选择 + 删除功能（已实现+编译通过）

- `adminofadmin.ui`：表格首列新增"选择"复选框列（列：选择/用户名/权限等级）；按钮行新增"删除管理员"。
- `adminofadmin.cpp`：refresh/search 首列复选框，用户名存 UserRole；修改权限改为勾选单条；新增 on_deleteBtn_clicked（勾选多条→禁止删 root/当前登录账号→二次确认→逐条参数化 DELETE→刷新）。
- `setLoginLevel` 更名为 `setLoginUser(name, level)`，同时控制"修改权限"与"删除管理员"按钮仅一级可见；adminpagewidget 调用处同步更新。
- 编译验证通过（shadow build，NexCard.exe 正常生成）。

## UI 优化方案（未改代码）

- 通读全部 .ui 与 res.qrc，输出方案文档 `docs/UI优化方案.md`。
- 核心结论：样式重复散落（5+ 份相同 QSS）、admin/员工主控 QSS 漂移、登录页绝对定位不可缩放、无状态反馈色、无图标、子窗口/弹窗风格脱节、无高 DPI。
- 方案：全局 app.qss 统一样式（Design Token）+ 登录页卡片化布局重构 + 主控页身份徽章/菜单图标 + 表格状态着色（考勤/权限/库存）+ 子窗口 QDialog 化 + 高 DPI，分 7 阶段实施。
- 已配套输出高保真样例图 `docs/UI优化样例.html`（登录页/主控页/管理员列表/弹窗/色板 5 个区块，可浏览器预览）。

## UI 优化实施（已全部完成 + 编译验证通过）

按方案 5 批次实施：
- **批次A 全局样式**：新建 `res/app.qss`（按钮/输入框/表格斑马纹/树/页签/滚动条/弹窗全控件统一，Design Token 主色 #4287d8）；res.qrc 注册；main.cpp 加载 qss + 高 DPI 属性 + 全局字体。
- **批次B 登录页重构**：widget/logincard/loginadmin/employeeregister 四个 .ui 全部改为布局管理器（删除绝对定位 geometry），登录子页 objectName=cardPanel 卡片样式，登录/注册按钮用 `primary="true"` 动态属性主色样式。
- **批次C 主控页**：python 脚本移除 admin/employee 主控 .ui 中 7 处内联 QSS（交全局接管）；welLabel 动态显示身份（管理员名+等级 / 员工姓名+卡号）；QTreeWidget 菜单加 QStyle 标准图标（注意 Qt5.12 无 SP_UserIcon，用 SP_DirHomeIcon）。
- **批次D 表格着色**：考勤签到绿/签退蓝、权限等级一级红加粗/二级蓝/三级灰、商品库存<=0红"缺货"/<=5橙；5 个业务表格 setAlternatingRowColors。
- **批次E 子窗口 QDialog 化**：adminofsetemp/adminofaddatt/adminofaddshop/adminofsetadd/adminofsetshop 5 个子窗口 .h/.cpp/.ui 全部 QWidget→QDialog（cpp 构造有 `QWidget(parent),` 和 `: QWidget(parent)` 两种写法都要替换）；补齐 3 处缺失的 WA_DeleteOnClose。
- 编译注意：Qt5.12 QStyle 无 SP_UserIcon；make 增量编译时中途修改源文件会导致不确定结果，应完成全部修改后一次性编译。
- 最终 shadow build 全量编译通过，NexCard.exe 正常生成。

## UI 优化修正：去除原背景图

按用户反馈（截图显示原 loginMag2.jpg / controlMag.jpg 背景图与 UI 优化样例风格不搭）：
- widget.cpp paintEvent：改为 QLinearGradient 浅蓝渐变（#f0f6fc → #e3eef6）
- adminpagewidget.cpp / employeepagewidget.cpp paintEvent：改为纯浅色（#f5f8fc）
- res.qrc 移除 loginMag2.jpg / controlMag.jpg 注册
- 删除 res/loginMag2.jpg / controlMag.jpg / LoginBackgroundImage.png 物理文件
- app.qss QMessageBox 加强：白底圆角 14px、默认按钮主色样式
- 编译通过，NexCard.exe 体积从 1.1MB 降到 603KB（去图）

## 弹窗比例修复

- 用户反馈：QMessageBox 弹窗很扁（高度不足、宽度过大）
- 原因：之前 QSS 给 `QMessageBox QLabel { min-width: 220px }` 强制 label 最小 220px 宽，导致弹窗被撑开
- 修复：移除 min-width，给 label 加 padding:12px 4px（避免文字贴边），给按钮加 min-height:26px（避免被压扁）；弹窗宽高完全由 Qt 根据文字+图标自动计算
- 编译通过。

## UI 贴近样例重构（登录页 + 欢迎页信息卡片）

用户反馈实际界面与 UI优化样例仍有差距，允许改控件。已重构：
- **登录页（widget.ui）**：整体白色圆角卡片（420px 居中）内含：渐变 Logo "N"（qlineargradient）+ 标题"新卡智联" + 副标题"企业一卡通考勤系统" + QPushButton 分段切换器（segment 动态属性 + checkable + autoExclusive，替代 RadioButton）+ QStackedWidget；窗口 600×400→600×520；三个登录子页根 objectName 由 cardPanel 改回类名（透明，避免双层卡片）。
- **欢迎页信息卡片**：admin/employee 主控欢迎 tab 改为 3 张信息卡（当前身份/串口状态/系统时间），QFrame#identityCard 等样式；cpp 用 QTimer 每秒刷新时间，串口状态读 isOpen()。
- **坑**：python 正则替换 .ui 时 pageTab 闭合标签计数错误（多出 </widget>），导致 uic 报 Opening/ending tag mismatch——需用 xml 校验；修复后正常。
- 编译通过，NexCard.exe 616KB。

## UI 继续贴近样例：程序图标 + 输入框图标

- **程序 Logo**：用 PIL 程序化生成 256×256 PNG（渐变蓝 #4287d8→#5b9bd5 + 白色粗体"N"，圆角 22%）和多尺寸 ICO（16/32/48/64/128/256），保存到 res/app.ico 和 res/icons/app.png；res.qrc 注册；删除原 titleMag.png 和无用的 loginMag.jpg；三个主窗口（widget/adminpage/employeepage）setWindowIcon 全部改为 :/res/app.ico。
- **副标题改 NexCard**：subLabel text 从"企业一卡通考勤系统"改为"NexCard"（与样例一致）；widget.cpp 增加 cardLayout setAlignment 让 Logo/标题/副标题水平居中。
- **登录子页加图标与提示**：
  - 三个子页（刷卡/管理员/员工注册）按样例重构：每行 [顶部 keyLabel] + [iconLabel 22×22 + lineEdit QHBoxLayout] + 按钮 + hintLabel。
  - 图标用 cpp 渲染 inline SVG（卡号/用户/锁/房子，实心主色蓝）；setSvgIcon() 通过 QSvgRenderer+QPainter+QPixmap 设置到 QLabel。
  - .pro 加 svg 模块。
- **坑**：手写 .ui XML 时连续多个 KeyLabel 后漏 </widget>；QSvgRenderer::render 只接受 QPainter*，需用 QPainter 中转渲染到 QPixmap。
- 编译通过，NexCard.exe 398KB。

## 登录界面调整与 QRadioButton 胶囊化

- 窗口高度 520→580；注册子页 380→440；verticalLayout spacing 10→14（每个字段之间留白加大）；确认/取消按钮 110→96 宽、spacer 40→30，避免"清空"被裁切。
- app.qss QRadioButton 重构为胶囊风格：未选中 #eef2f7 淡灰底 + #d5dde7 灰边 + 14px 圆角；选中 :checked 转白底 #4287d8 蓝边 + 加粗文字 + indicator 蓝色实心点（qradialgradient）；min-height:26px + padding:4px 14px 让整体更像 toggle button。
- 编译通过。

## 修正：分段按钮胶囊化 + 性别单选恢复普通 + 窗口调宽

- 用户澄清：要求改的是登录页分段按钮（刷卡登录/管理员登录/员工注册），而非性别 QRadioButton。
- app.qss：分段按钮 QPushButton[segment="true"]:checked 改为白底 #ffffff + 蓝边 #4287d8 + 蓝字 + 加粗（之前是蓝底白字）。
- 性别 QRadioButton 恢复普通：去掉胶囊 padding/background，只保留 indicator 16×16 圆圈 + qradialgradient 蓝点中心。
- 窗口 600→640；cardPanel 420→460；注册子页 420→460，让"清空"按钮不再被裁切。
- 编译通过。

## 员工注册页：去图标 + label 移到编辑框前

- employeeregisterwidget.ui 重写：删除所有 cardIconLabel/nameIconLabel/deptIconLabel（取消小图标）；每个字段改为水平 QHBoxLayout [Label + Input]：
  - 卡号行：[卡号：] [cardLineEdit]
  - 姓名/年龄行：[姓名：] [nameLineEdit] [年龄：] [ageLineEdit]（年龄 label 单独前置）
  - 性别行：[性别：] [男] [女]
  - 部门行：[部门：] [departmentLineEdit]
  - 按钮行居中布局
- employeeregisterwidget.cpp：删除 setSvgIcon 函数与 QSvgRenderer/QPixmap/QPainter include；删除三处 setSvgIcon 调用。
- 其他子页（刷卡登录/管理员登录）保留图标不动。
- 编译通过，NexCard.exe 397KB。

## 姓名框缩短 + 分段按钮圆润化

- employeeregisterwidget.ui：nameLineEdit width 150→110，ageLineEdit width 80→100，让姓名/年龄输入框之间留出视觉间距避免重合。
- app.qss 分段按钮圆角 10→14，更圆润。
- 编译通过。

## 员工注册页比例修复（保留历史记录）

- 用户反馈：字段间距过紧（label 与 input 几乎贴在一起）、单选框 indicator 显大（"（ ）"括号状）、输入框文字显示异常。
- employeeregisterwidget.ui：verticalLayout spacing=10；子页 height 340→380；所有 lineEdit minimumSize height 34（之前 0）；sexLayout spacing=24 + leftMargin=4。
- app.qss：QRadioButton 加 min-height:22px+padding:2px 0；indicator 16×16（之前 18）+ border 1.5px；checked 改用 qradialgradient 模拟"中心蓝点"效果（更像样例的圆圈 + 中心实心点）；QCheckBox 同步缩小。
- 编译通过。

## 锁定 DockWidget 与修复欢迎页身份

- adminpagewidget.cpp / employeepagewidget.cpp 构造：`ui->ngtDockWidget->setFeatures(QDockWidget::NoDockWidgetFeatures)`——禁止菜单栏拖出/脱离/关闭按钮。
- employeepagewidget.cpp 构造里去掉"查姓名"逻辑（构造时卡号空），改默认显示"您好，员工/未识别"；新增 `setLoginInfo()` 方法，由 widget.cpp lambda 在 `LoginCardWidget::loginOK` 触发后调用，更新姓名/卡号到 welLabel + identityValueLabel，并刷新 serialValueLabel。
- widget.cpp 员工刷卡后：`ew->setLoginInfo();` 然后隐藏登录窗。
- adminpagewidget.cpp / employeepagewidget.cpp 构造末尾：`ui->pageTabWidget->tabBar()->setTabButton(0, QTabBar::RightSide, nullptr)` 隐藏欢迎页 tab 的关闭按钮（其他 tab 仍可关闭）。
- 编译通过。

- myserial.cpp initSerialProt()：setPortName("COM5") → ("COM7")，波特率 115200 8N1 不变。
- 全项目 grep 确认无其他代码硬编码 COM5（docs 文档为历史描述，未动）。
- 编译通过。

## 串口关闭提示与欢迎页串口实时刷新

- serialportwidget.cpp on_closeBtn_clicked：if 分支（已关闭成功）追加 QMessageBox::information "串口已关闭"，给用户视觉反馈（之前只有 appendLog，无 MessageBox，可能用户没注意到）。
- adminpagewidget.cpp / employeepagewidget.cpp 构造中 timeValueLabel QTimer lambda 内增加串口状态实时刷新：每秒检查 `port->isOpen()`，打开/关闭后欢迎页立即同步。
- 编译通过。

## 取消"串口未打开"提示框

- serialportwidget.cpp on_closeBtn_clicked：删除 else 分支的 QMessageBox::warning "串口未打开"（原因：关闭成功弹"串口已关闭"模态框后，用户第二次点击 close 按钮会进入 else 分支弹出两个提示框）。未打开时不再弹窗。
- 编译通过。

- adminpagewidget.cpp setupParentToggleIcon：监听 `QTreeWidget::itemExpanded/Collapsed`，根据父项 isExpanded() 切换图标为 `SP_DirOpenIcon`（展开）/ `SP_DirIcon`（收起）。坑：connect context 不能用 QTreeWidgetItem（不是 QObject 子类），改用 tree 作为 context。
- mysql.cpp creatMySql：检测 product 表为空时，用 SQLite `WITH RECURSIVE cnt(x) AS (...)` + CASE/printf 一次性插入 50 条商品（5 类 × 10 编号：矿泉水/可乐/方便面/薯片/牛奶）。
- Python sqlite3 验证种子 SQL：插入 50 条成功，样例 id=1 可乐_1 ¥3.00 库存13。
- 编译通过。

## 版本备份：v1.0 / v2.0

- 旧备份 `Smart_Card_Pass_backup_20260821_1641`（85 文件，UI 优化前）重命名为 **`Smart_Card_Pass_v1.0`**。
- 当前项目（88 文件，UI 优化+三级权限+商品种子等全部完成）完整复制为 **`Smart_Card_Pass_v2.0`**，diff -r 校验与源目录完全一致。

- serialportwidget.cpp 构造函数：删除 `m_port->close()`（进入串口配置页不再自动关闭已打开串口）；新增逻辑：若当前串口已打开，下拉框默认选中该端口、closeBtn 可用/openBtn 禁用（按 isOpen 状态初始化按钮）。
- myserial.cpp searchSerialPort：去掉逐个 `port.open()` 测试（部分虚拟/蓝牙串口 open 会阻塞导致刷新卡顿），改为直接列出 `QSerialPortInfo::availablePorts()`；被占用端口仍列出，打开失败由 open() 报错。
- 编译通过。
